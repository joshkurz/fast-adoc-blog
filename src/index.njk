---

title: Home
layout: base.njk
pagination:
  data: collections.adoc
  size: 5
  alias: posts
---
<section class="hero">
  <div class="wrap">
    <h2 class="title">Welcome</h2>
    <p class="subtitle">Fast, clean posts rendered with AsciiDoc. Dive into the latest articles, tips, and notes.</p>
  </div>
</section>

<section class="wrap">
  <div class="posts-grid">
    {% for post in posts %}
      {% include "post-card.njk" %}
    {% endfor %}
  </div>

  <nav style="margin-top:1rem;">
    {% if pagination.href.previous %}
      <a href="{{ pagination.href.previous }}">Newer posts</a>
    {% endif %}
    {% if pagination.href.next %}
      {% if pagination.href.previous %} · {% endif %}
      <a href="{{ pagination.href.next }}">Older posts</a>
    {% endif %}
  </nav>
</section>
