---

title: Home
layout: base.njk
pagination:
  data: collections.adoc
  size: 5
  alias: posts
---
<section class="hero">
  <div class="wrap">
    <h2 class="title">Welcome</h2>
    <p class="subtitle">Fast, clean posts rendered with AsciiDoc. Dive into the latest articles, tips, and notes.</p>
  </div>
</section>

<section class="wrap">
  <div class="posts-grid">
    {% for post in posts %}
      {% set img = post.data.image or post.data.cover or post.data.hero or post.data.thumbnail %}
      {% set author = post.data.author or post.data.byline %}
      {% set desc = post.data.description or post.data.excerpt or (post.templateContent | excerpt) %}
      <article class="card{% if not img %} no-image{% endif %}">
        <a class="card-image-link" href="{{ post.url }}" aria-label="{{ post.data.title }}">
          {% if img %}<img class="card-image" src="{{ img }}" alt="">{% endif %}
        </a>
        <div class="card-content">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p class="card-meta">{{ post.date | readableDate }}{% if author %} · {{ author }}{% endif %}</p>
          <p class="card-excerpt">{{ desc }}</p>
          <a class="card-cta" href="{{ post.url }}">Read more →</a>
        </div>
      </article>
    {% endfor %}
  </div>

  <nav style="margin-top:1rem;">
    {% if pagination.href.previous %}
      <a href="{{ pagination.href.previous }}">Newer posts</a>
    {% endif %}
    {% if pagination.href.next %}
      {% if pagination.href.previous %} · {% endif %}
      <a href="{{ pagination.href.next }}">Older posts</a>
    {% endif %}
  </nav>
</section>
